version: '2'

services:
  mailhog:
    image: mailhog/mailhog
    environment:
      - TERM=xterm
    labels:
      - "traefik.backend=mailhog"
      - "traefik.port=8025"
      - "traefik.frontend.rule=Host:mailhog.harpoon.dev"
      - "traefik.frontend.entryPoints=http"
      - "traefik.docker.network=harpoon"
      - "traefik.tags=harpoon"
    networks:
      default:
        aliases:
          - mailhog.harpoon.dev
    ports:
      - "1025:1025"
      - "8025"

networks:
  default:
    external:
      name: harpoon